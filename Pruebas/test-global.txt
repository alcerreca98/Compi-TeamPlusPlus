program Team_pp;

class Persona{
	attributes
		edad:int;
		nombre[30]:char;
	methods
		int func UNO(x : int){
			return(edad - x);
		}
}

var i, j, p : int;
var estudiante : Persona;

int func fact(j : int)
var i : int;{
	i = j+(p-j*2+j);
	if(j==1)then{
		return(j);
	}
	else{
		return(j*fact(j-1));
	}
}

void func pelos(y : int)
var x : int;{
	read(estudiante->edad);
	x = y;
	while(x < 10) do{
		write(estudiante->UNO(x));
		x = x+1;
	}
}

main(){
	read(p); j=p*2;
	i = fact(p);
	read(estudiante->nombre);
	pelos(p);
	for i = 1 to 10 do {
		write("HelloWorld",alumno->nombre, fact(alumno->edad));
	}
}