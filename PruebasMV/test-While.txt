program Team_pp;

main()
   var int: x, y;
{   
    x=0;
    y=0;
    while(x <= 2) do{
        x = x+1;
        y = 0;
        while(y != 3) do{
            y = y+1;
            write(y);
        }
        write("x es igual a: ",x);
    }
}